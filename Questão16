#include <iostream>

using namespace std;

// Troca valores por referência
void trocar(int& a, int& b) {
    int temp = a;
    a = b;
    b = temp;
}

int main() {
    int x = 5, y = 10;

    cout << "Antes da troca: x = " << x << ", y = " << y << endl;
    trocar(x, y);
    cout << "Depois da troca: x = " << x << ", y = " << y << endl;

    return 0;
}
