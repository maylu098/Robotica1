#include <iostream>

using namespace std;

void exibirVetor(int* vetor) {
    for (int i = 0; i < 5; i++) {
        cout << "Elemento " << i << ": " << *(vetor + i) << endl;
    }
}

int main() {
    int numeros[5] = {1, 2, 3, 4, 5};
    exibirVetor(numeros);
    return 0;
}
